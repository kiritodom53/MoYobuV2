<?xml version="1.0" encoding="utf-8"?>

<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:local="clr-namespace:MoYobuV2.ChapterViewer"
             Padding="0"
             x:Class="MoYobuV2.ChapterViewer.MainPage">

    <ScrollView Margin="0" Padding="0" BackgroundColor="Aquamarine">
        <!-- <StackLayout x:Name="ImagesContainer" BackgroundColor="Black" Margin="0" Padding="0" -->
        <!-- HorizontalOptions="FillAndExpand" -->
        <!-- VerticalOptions="FillAndExpand" /> -->
        <StackLayout x:Name="ImagesContainer" BackgroundColor="LightPink" Margin="0" Padding="0"
                     HorizontalOptions="FillAndExpand"
                     VerticalOptions="FillAndExpand">
            <Image 
                x:Name="Img1"
                Aspect="Fill"
                HorizontalOptions="Center"
                VerticalOptions="Center"
                />
            <!-- <Grid BackgroundColor="Black" Margin="0" Padding="0" -->
            <!--       HorizontalOptions="FillAndExpand" -->
            <!--       VerticalOptions="FillAndExpand" /> -->
        </StackLayout>
    </ScrollView>

</ContentPage>